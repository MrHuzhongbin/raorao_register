# 应用名称
spring:
  application:
    name: raorao_register
server:
  port: 10086
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka
    fetch-registry: false #是否注册自己
    register-with-eureka: false #是否拉取服务列表
